.gradle
.idea
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

### Runtime data (regenerated locally, not source) ###
# H2 database files used by the JPA and Exposed CRUD demos.
data/
# Dolt's own local repository/version-control state and server runtime config -
# regenerate with `dolt init` + schema, see README.md. Keep doltdb/config.yaml (server config)
# and doltdb/*.sql (schema/seed scripts) tracked.
doltdb/.dolt/
doltdb/.doltcfg/
# `dolt sql-server` also regenerates these at the repo root if run from here without
# -r/--config flags (default config template + privileges db) - not source, not needed.
/.doltcfg/
/config.yaml

### IntelliJ IDEA ###
*.iws
*.iml
*.ipr
out/
!**/src/main/**/out/
!**/src/test/**/out/

### Kotlin ###
.kotlin

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
!**/src/main/**/bin/
!**/src/test/**/bin/

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store